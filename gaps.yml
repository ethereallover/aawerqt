apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ hengsheng }}
  name: {{ APP_NAME }}-qweq
  labels:
    app: {{ APP_NAME }}-qweq
spec:
  selector:
    matchLabels:
      app: {{ APP_NAME }}-qweq
  template:
    metadata:
      labels:
        app: {{ APP_NAME }}-qweq
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
                - matchExpressions:
                  - key: {{ sss }}
                    operator: In
                    values:
                      - {{ dsf }}
      initContainers:
        - name: init-myapp
          image: 103.160.119.30/daocloud/busybox:v3
          command: ['sh']
          args: ['-c', 'chown -R gaps.gaps /img; chown -R gaps.gaps /logs']
          volumeMounts:
            - mountPath: /logs
              name: app-logs
            - mountPath: /img
              name: app-imgs
      imagePullSecrets:
      	- name: images-103.160.119.30
      containers:
        - image: {{ qwe.image }}
          name: {{ APP_NAME }}-qweq
          securityContext:
            runAsUser: 810
          resources:
            requests:
              memory: "0"
              cpu: "0"
            limits:
              memory: "0"
              cpu: "0"
          ports:
            - containerPort: 9080
          env:
            - name: SW_AGENT_NAME
              value: {{ APP_NAME }}-qweqwe
            - name: APOLLO_APP_ID
              value: {{ apollo-config-group }}
            - name: oid
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: DCE_POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP  
          envFrom:
            - configMapRef:
                name: dmp-user-config
          livenessProbe:
            tcpSocket:
              port: 9080
            initialDelaySeconds: 30
            timeoutSeconds: 5
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 9080
            initialDelaySeconds: 30
            timeoutSeconds: 5
            periodSeconds: 15
          volumeMounts:
            - name: app-imgs
              mountPath: /qweq/data
            - name: app-logs
              mountPath: /logs
      volumes:
        - name: host-time
          hostPath:
            path: /etc/localtime
        - name: app-logs
          hostPath:
            path: /data/qweq/qweq-log
        - name: app-imgs
          hostPath:
            path: /data/qweq/qweq-data